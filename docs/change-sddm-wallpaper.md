# วิธีเปลี่ยน SDDM Login Screen Wallpaper

## 🎨 วิธีง่าย: เปลี่ยนผ่าน variables.nix

### ขั้นตอนที่ 1: เลือกรูปภาพ
ดูรูปที่มีอยู่ใน wallpapers folder:
```bash
ls ~/NixOS/modules/themes/wallpapers/
```

รูปที่มีอยู่:
- `yns4.jpg` - รูปที่กำลังใช้อยู่
- `yns3.jpg`
- `kurzgesagt.webp`
- `kurzgesagt-galaxies.webp`
- `evening-sky.webp`
- `cyberpunk.png`
- `train.jpg`
- และอื่นๆ

### ขั้นตอนที่ 2: เปลี่ยนค่าใน variables.nix
เปิดไฟล์ `~/NixOS/hosts/makufff/variables.nix` และเปลี่ยน `sddmWallpaper`:

```nix
sddmWallpaper = "ชื่อไฟล์รูป.jpg"; # เช่น "yns4.jpg", "evening-sky.webp"
```

### ขั้นตอนที่ 3: เพิ่มรูปใหม่ (ถ้าต้องการ)
1. Copy รูปของคุณไปไว้ที่:
   ```bash
   cp /path/to/your/image.jpg ~/NixOS/modules/themes/wallpapers/
   ```

2. แล้วแก้ไข `sddmWallpaper` ให้ตรงกับชื่อไฟล์:
   ```nix
   sddmWallpaper = "image.jpg";
   ```

### ขั้นตอนที่ 4: Apply การเปลี่ยนแปลง
```bash
cd ~/NixOS
sudo nixos-rebuild switch --flake .#makufff
```

### ขั้นตอนที่ 5: ทดสอบ
Logout เพื่อดูหน้า login screen ใหม่:
```bash
hyprctl dispatch exit
```

## 🎯 ตัวอย่างการใช้งาน

### ใช้รูป evening-sky:
```nix
sddmWallpaper = "evening-sky.webp";
```

### ใช้รูป kurzgesagt galaxies:
```nix
sddmWallpaper = "kurzgesagt-galaxies.webp";
```

### ใช้รูปของตัวเอง:
```bash
# Copy รูปไปไว้ใน wallpapers
cp ~/Downloads/my-wallpaper.jpg ~/NixOS/modules/themes/wallpapers/

# แก้ไข variables.nix
sddmWallpaper = "my-wallpaper.jpg";

# Rebuild
sudo nixos-rebuild switch --flake .#makufff
```

## 💡 Tips

- รูปควรมีความละเอียดไม่ต่ำกว่า 1920x1080 เพื่อความคมชัด
- รูปไม่ควรมีขนาดไฟล์ใหญ่เกิน 5MB
- รูปควรมีโทนสีที่เหมาะสม (ไม่สว่างหรือมืดจนเกินไป)
- สามารถใช้ไฟล์ .jpg, .png, .webp ได้

## 🔧 Troubleshooting

**ปัญหา: รูปไม่แสดง**
- ตรวจสอบว่าชื่อไฟล์ถูกต้อง (case-sensitive)
- ตรวจสอบว่ารูปอยู่ใน `modules/themes/wallpapers/` จริง
- Rebuild อีกครั้ง

**ปัญหา: รูปผิดเพี้ยน**
- ตรวจสอบ aspect ratio ของรูป ควรเป็น 16:9
- ลองใช้รูปที่มีความละเอียดสูงกว่า

## 📝 Theme Options

นอกจากเปลี่ยนรูปแล้ว ยังสามารถเปลี่ยน theme ได้:

```nix
sddmTheme = "astronaut";     # นักบินอวกาศ (รองรับ custom wallpaper)
sddmTheme = "black_hole";    # หลุมดำ
sddmTheme = "purple_leaves"; # ใบไม้สีม่วง
sddmTheme = "jake_the_dog";  # Jake จาก Adventure Time
sddmTheme = "hyprland_kath"; # Hyprland themed
```

**หมายเหตุ:** ตอนนี้ custom wallpaper ใช้ได้กับ theme `astronaut` เท่านั้น
